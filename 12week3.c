#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main() {
    int K, N, numbers[10000];
    scanf("%d %d", &K, &N);
    for (int i = 0; i < K; i++) {
        scanf("%d", &numbers[i]);
    }
    

    
}
